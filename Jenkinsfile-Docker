pipeline {
    agent {
        dockerContainer { image 'node:20.9.0-alpine3.18' }
    }
    stages {
        stage('Debug') {
            steps {
                sh 'docker version'
                sh 'docker images'
                sh 'node --version'
            }
        }
    }
}