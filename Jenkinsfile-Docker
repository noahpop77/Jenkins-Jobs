pipeline {
    agent {
        docker { 
            image 'node:20.9.0-alpine3.18' 
            }
    }
    stages {
        stage('Debug') {
            steps {
                sh 'node --version'
            }
        }
    }
}